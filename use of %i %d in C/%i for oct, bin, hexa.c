//----------------------------------------------------------------------------

#include<stdio.h>
int main(){
    int x;
    scanf("%i",&x);
    printf("%d",x);
}

/*
    When you run this program, you can input values in different formats:
    *Decimal: Input a regular decimal number, like 42.
    *Octal: Input an octal number, which 
           starts with 0, like 052 (which is 42 in decimal).
    *Hexadecimal: Input a hexadecimal number, which 
           starts with 0x or 0X, like 0x2A (which is also 42 in decimal).
*/
//-----------------------------------------------------------------------------
